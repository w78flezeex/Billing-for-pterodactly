# 🚀 Инструкция по установке Host Template

Пошаговое руководство по установке и настройке шаблона хостинг-сайта.

## 📋 Требования

- **Node.js** версии 18.0 или выше
- **npm** или **yarn** пакетный менеджер
- **Git** для клонирования репозитория

## 🛠 Установка

### Шаг 1: Устоновка проекта

# Перейдите в папку проекта
cd host-template
\`\`\`

# Загрузите репозиции в папку
cd /var/www/host


### Шаг 2: Установка зависимостей

\`\`\`bash
# Установите зависимости через npm
npm install

# Или через yarn
yarn install
\`\`\`

### Шаг 3: Запуск в режиме разработки

\`\`\`bash
# Запустите сервер разработки
npm run dev

# Или через yarn
yarn dev
\`\`\`

## ⚙️ Настройка сайта

### 1. Основные настройки

Откройте файл `lib/config.ts` и измените основные параметры:

\`\`\`typescript
export const siteConfig = {
  // Название вашего хостинга
  siteName: "YourHosting",
  
  // Описание на разных языках
  siteDescription: {
    ru: "Ваше описание на русском языке",
    en: "Your description in English",
    ua: "Ваш опис українською мовою",
  },
  
  // Контактная информация
  contacts: {
    email: "support@yourhosting.com",
    telegram: "@yourhosting_support",
    discord: "discord.gg/yourhosting",
  },
}
\`\`\`

### 2. Настройка тарифов

В том же файле найдите секцию `pricingPlans` и настройте свои тарифы:

\`\`\`typescript
export const pricingPlans = {
  game: [
    {
      name: "Starter",
      description: {
        ru: "Для начинающих проектов",
        en: "For beginner projects",
        ua: "Для початківців",
      },
      price: { monthly: 299, yearly: 2990 },
      features: {
        ru: ["2 GB RAM", "20 GB SSD", "DDoS защита"],
        en: ["2 GB RAM", "20 GB SSD", "DDoS Protection"],
        ua: ["2 GB RAM", "20 GB SSD", "DDoS захист"],
      },
      popular: false,
    },
    // Добавьте больше тарифов...
  ],
  // Настройте VPS, веб-хостинг и выделенные серверы...
}
\`\`\`

### 3. Настройка отзывов

Измените отзывы клиентов в секции `testimonials`:

\`\`\`typescript
export const testimonials = [
  {
    name: {
      ru: "Иван Петров",
      en: "John Smith",
      ua: "Іван Петров",
    },
    role: {
      ru: "Владелец игрового сервера",
      en: "Game Server Owner",
      ua: "Власник ігрового сервера",
    },
    content: {
      ru: "Отличный сервис! Рекомендую всем.",
      en: "Great service! Recommend to everyone.",
      ua: "Відмінний сервіс! Рекомендую всім.",
    },
    rating: 5,
    source: "Discord",
  },
]
\`\`\`

### 4. Настройка дополнительных услуг

Измените список услуг в секции `services`:

\`\`\`typescript
export const services = [
  {
    title: {
      ru: "Автоматические бэкапы",
      en: "Automatic Backups",
      ua: "Автоматичні бекапи",
    },
    description: {
      ru: "Ежедневное резервное копирование данных",
      en: "Daily data backup",
      ua: "Щоденне резервне копіювання",
    },
  },
  // Добавьте больше услуг...
]
\`\`\`

## 🎨 Кастомизация

### Изменение цветовой схемы

Откройте `tailwind.config.ts` и измените цвета:

\`\`\`typescript
theme: {
  extend: {
    colors: {
      primary: {
        DEFAULT: "hsl(221.2 83.2% 53.3%)", // Основной цвет
        foreground: "hsl(210 40% 98%)",
      },
      // Другие цвета...
    },
  },
}
\`\`\`

### Изменение шрифтов

В файле `app/layout.tsx` измените шрифт:

\`\`\`typescript
import { Inter, Roboto } from 'next/font/google'

const roboto = Roboto({ 
  subsets: ["latin", "cyrillic"],
  weight: ["300", "400", "500", "700"]
})
\`\`\`

### Добавление логотипа

1. Поместите файл логотипа в папку `public/`
2. В `components/header.tsx` замените иконку:

\`\`\`typescript
// Замените эту строку:
<Server className="h-8 w-8 text-primary" />

// На эту:
<img src="/your-logo.png" alt="Logo" className="h-8 w-auto" />
\`\`\`

## 🌐 Добавление нового языка

### Шаг 1: Добавьте переводы

В файле `lib/translations.ts` добавьте новый язык:

\`\`\`typescript
export const translations = {
  ru: { /* существующие переводы */ },
  en: { /* существующие переводы */ },
  ua: { /* существующие переводы */ },
  de: { // Новый язык
    about: "Über uns",
    pricing: "Preise",
    // Добавьте все переводы...
  },
}
\`\`\`

### Шаг 2: Обновите тип языка

\`\`\`typescript
export type Language = "ru" | "en" | "ua" | "de"
\`\`\`

### Шаг 3: Добавьте контент в config.ts

\`\`\`typescript
siteDescription: {
  ru: "Описание на русском",
  en: "Description in English", 
  ua: "Опис українською",
  de: "Beschreibung auf Deutsch", // Новый язык
},
\`\`\`

### Шаг 4: Обновите переключатель языков

В `components/header.tsx` добавьте новую опцию:

\`\`\`typescript
<DropdownMenuContent>
  <DropdownMenuItem onClick={() => handleLanguageChange("ru")}>🇷🇺 Русский</DropdownMenuItem>
  <DropdownMenuItem onClick={() => handleLanguageChange("en")}>🇺🇸 English</DropdownMenuItem>
  <DropdownMenuItem onClick={() => handleLanguageChange("ua")}>🇺🇦 Українська</DropdownMenuItem>
  <DropdownMenuItem onClick={() => handleLanguageChange("de")}>🇩🇪 Deutsch</DropdownMenuItem>
</DropdownMenuContent>
\`\`\`

## 📱 Адаптивность

Сайт автоматически адаптируется под все устройства. Для дополнительной настройки используйте Tailwind CSS классы:

\`\`\`typescript
// Пример адаптивных классов
className="text-sm md:text-base lg:text-lg xl:text-xl"
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
className="px-4 md:px-6 lg:px-8"
\`\`\`

## 🚀 Деплой

### Vercel (рекомендуется)

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Подключите ваш GitHub репозиторий
3. Vercel автоматически деплоит ваш сайт

### Netlify

1. Создайте аккаунт на [Netlify](https://netlify.com)
2. Подключите репозиторий
3. Настройте команды сборки:
   - Build command: `npm run build`
   - Publish directory: `out`

### Собственный сервер

\`\`\`bash
# Соберите проект
npm run build

# Запустите в продакшене
npm start
\`\`\`

## 🔧 Полезные команды

\`\`\`bash
# Разработка
npm run dev          # Запуск сервера разработки
npm run build        # Сборка для продакшена
npm run start        # Запуск продакшен сервера
npm run lint         # Проверка кода

# Очистка
rm -rf .next         # Очистка кэша Next.js
rm -rf node_modules  # Удаление зависимостей
npm install          # Переустановка зависимостей
\`\`\`

## 📁 Структура проекта

\`\`\`
host-template/
├── app/                    # Next.js App Router
│   ├── globals.css        # Глобальные стили
│   ├── layout.tsx         # Основной layout
│   └── page.tsx           # Главная страница
├── components/            # React компоненты
│   ├── ui/               # UI компоненты (shadcn/ui)
│   ├── header.tsx        # Шапка сайта
│   ├── hero-section.tsx  # Главная секция
│   ├── pricing-section.tsx # Тарифы
│   └── ...               # Другие компоненты
├── lib/                  # Утилиты и конфигурация
│   ├── config.ts         # Основная конфигурация
│   ├── translations.ts   # Переводы
│   └── utils.ts          # Вспомогательные функции
├── hooks/                # React хуки
│   └── use-language.ts   # Хук для работы с языками
├── public/               # Статические файлы
└── package.json          # Зависимости проекта
\`\`\`

## ❓ Часто задаваемые вопросы

### Q: Как изменить валюту?

A: В файле `components/pricing-section.tsx` найдите символ `₽` и замените на нужную валюту:

\`\`\`typescript
<span className="text-4xl font-bold">{plan.price[billingPeriod]}$</span>
\`\`\`

### Q: Как добавить новую секцию?

A: 1. Создайте новый компонент в `components/`
2. Добавьте его в `app/page.tsx`
3. Добавьте ссылку в навигацию в `components/header.tsx`

### Q: Как изменить анимации?

A: Анимации используют Framer Motion. Измените параметры в компонентах:

\`\`\`typescript
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.5 }}
>
\`\`\`

### Q: Как добавить Google Analytics?

A: Добавьте скрипт в `app/layout.tsx`:

\`\`\`typescript
<Script src="https://www.googletagmanager.com/gtag/js?id=GA_TRACKING_ID" />
<Script id="google-analytics">
  {`
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'GA_TRACKING_ID');
  `}
</Script>
\`\`\`

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте [документацию Next.js](https://nextjs.org/docs)
2. Посмотрите [примеры Tailwind CSS](https://tailwindcss.com/docs)
3. Обратитесь ко мне: **@prd_yt**

## 📄 Лицензия

MIT License - вы можете свободно использовать, изменять и распространять этот шаблон.

---

**Автор:** @prd_yt  
**Версия:** 1.0.0  
**Последнее обновление:** 2025
